export enum OrderStatus {
    IN_PROGRESS = "IN_PROGRESS",
    NEW = "NEW", 
}

export enum Gender {
    MALE = "MALE",
    FEMALE = "FEMALE",
}

export enum Condition {
    NEW = "NEW",
    UNKNOWN = "UNKNOWN",
}
